# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# production
/build

# private master data (contains PII) - never commit
/data

# Per-respondent microdata must NEVER be published. Only public/shares_agg.json
# (computed aggregates, no respondent rows) is committed/deployed. The local
# de-identified microdata lives in /data (above) and is git-ignored.
/public/Shares_RD.xlsx

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
